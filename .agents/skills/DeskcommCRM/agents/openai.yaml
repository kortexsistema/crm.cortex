interface:
  display_name: "Kortex"
  short_description: "Repo-specific patterns and workflows for Kortex"
  default_prompt: "Use the Kortex repo skill to follow existing architecture, testing, and workflow conventions."
policy:
  allow_implicit_invocation: true